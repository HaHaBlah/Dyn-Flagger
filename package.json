{
  "scripts": {
    "dev:vite": "vite",
    "dev": "npm run build && wrangler pages dev dist",
    "build": "vite build"
  },
  "dependencies": {
    "express": "^5.2.1",
    "luaparse": "^0.3.1"
  },
  "devDependencies": {
    "vite": "^7.3.1",
    "wrangler": "^4.62.0"
  }
}
